import { ElementEvadeDirective } from './element-evade.directive';

describe('ElementEvadeDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementEvadeDirective();
    expect(directive).toBeTruthy();
  });
});
